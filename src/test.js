import gsap from 'gsap';
import {ScrollTrigger} from "gsap/ScrollTrigger";

gsap.registerPlugin(ScrollTrigger);





//toggleActions : play pause resume reverse restart reset complete none
// for section for toggle Actions : (onEnter onLeave onEnterBack onLeaveBack)


//values : top center bottom and pixels
// if add pixels its taken from the top
//start & end to values (triggerElement Screen)
//if we want to make the end is relative to the start we add this "+=300" after start with 300px
//we can add endTrigger or startTrigger and make the start and end relative to it
//scrub animation start the animation based on the scroll can take true or take a number
//pin keep the element where it is like



//gsap TimeLine
